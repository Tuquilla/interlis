# Interlis
Basic Library to consume Interlis-Files (.xtf) and extract geometries.
This is an early pre-alpha version!

## Usage
Use `ReadGeometry(decoder *xml.Decoder)` to read out geometries from xtf.